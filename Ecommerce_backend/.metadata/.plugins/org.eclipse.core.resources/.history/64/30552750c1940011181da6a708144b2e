spring.application.name=Ecommerce_Backend

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# MySQL Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/ecommerce?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false}
spring.datasource.username=root
spring.datasource.password=(^&@%(&*(!)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Spring Security & Autoconfiguration
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Razorpay Configuration
razorpay.key_id=${RAZORPAY_KEY_ID:rzp_test_T6aWVQ3c0fO67L}
razorpay.key_secret=${RAZORPAY_KEY_SECRET:93C1yDpJSyizvtXJYdd2POWx}

# Mail Configuration
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:stitchwave.noreply@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:bykf vipd zhhi wizp}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true